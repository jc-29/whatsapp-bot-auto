services:
  - type: web
    name: whatsapp-spreadsheet-bot
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    healthCheckPath: /api/status
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_OPTIONS
        value: "--expose-gc --max-old-space-size=192"
