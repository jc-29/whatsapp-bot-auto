services:
  - type: web
    name: whatsapp-spreadsheet-bot
    env: docker
    plan: free
    dockerfilePath: Dockerfile
    healthCheckPath: /api/status
    envVars:
      - key: PORT
        value: 3000
